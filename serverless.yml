service: hello

useDotenv: true

custom:
  esbuild:
    bundle: true
    minify: true

provider:
  name: aws
  runtime: nodejs14.x

functions:
  hello:
    handler: src/hello.handler
    events:
      - httpApi: GET /hello

plugins:
  - serverless-offline
  - serverless-esbuild